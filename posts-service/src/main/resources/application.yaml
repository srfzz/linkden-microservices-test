
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/linkden_posts_db
    username: postgres
    password: "123456789@@"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8083